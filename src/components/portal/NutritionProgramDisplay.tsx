// Stub: NutritionProgramDisplay — requires nutrition_programs table
// Full implementation will be ported once that table is created

import { Loader2 } from "lucide-react";

interface NutritionProgramDisplayProps {
  playerId: string;
}

export const NutritionProgramDisplay = ({ playerId }: NutritionProgramDisplayProps) => {
  return (
    <div className="text-center py-8 text-muted-foreground text-sm">
      Nutrition program display will be available once nutrition data is configured.
    </div>
  );
};
